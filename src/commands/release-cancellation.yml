description: >
  Perfoms a release cancellation

parameters:
  NEXUS_USERNAME:
    type: env_var_name
    default: NEXUS_USERNAME
    description: "Nexus Username"
  NEXUS_PASSWORD:
    type: env_var_name
    default: NEXUS_PASSWORD
    description: "Nexus Password"
steps:
  - run:
      name: Retrievestaging repository ID and drop staging repository on Nexus
      command: |
        echo <<parameters.NEXUS_USERNAME>>
        echo ${<<parameters.NEXUS_USERNAME>>}
        echo $<<parameters.NEXUS_USERNAME>>
