description: >
  Sample description
# What will this job do?
# Descriptions should be short, simple, and clear.

executor: default

parameters:
  RESOURCE_CLASS:
    type: string
    default: "large"
    description: "Resource class to be used for running the job"
  MODULE_ID:
    type: string
    default: "large"
    description: "Resource class to be used for running the job"
  RELEASE_VERSION:
    type: string
    description: "Version to cancel"

working_directory: ~/source

docker:
  - image: cimg/openjdk:8.0.275-node

resource_class: << parameters.RESOURCE_CLASS >>

steps:
  - release-cancellation:
      MODULE_ID: << parameters.MODULE_ID >>
      RELEASE_VERSION: << parameters.RELEASE_VERSION >>
